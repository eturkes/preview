{
  "name": "@in-progress/preview",
  "version": "0.2.0",
  "description": "Generate evidence-grounded bilingual project dashboards with Codex.",
  "private": true,
  "license": "Apache-2.0 WITH LLVM-exception",
  "type": "module",
  "scripts": {
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm runtime:check",
    "format": "oxfmt --write src test tools package.json tsconfig.json",
    "format:check": "oxfmt --check src test tools package.json tsconfig.json",
    "lint": "oxlint --deny-warnings --type-aware --type-check src test tools",
    "runtime:check": "bun tools/plugin-runtime-test.ts",
    "test": "bun test ./test/",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "jsonc-parser": "3.3.1"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "bun": "1.3.14",
    "oxfmt": "0.63.0",
    "oxlint": "1.78.0",
    "oxlint-tsgolint": "7.0.2001",
    "typescript": "7.0.2"
  },
  "engines": {
    "bun": ">=1.3.14",
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@11.21.0"
}
